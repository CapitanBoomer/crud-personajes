import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'starwars Crud',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
